<div class="jumbotron">
    <h1>Cheeriongoose</h1>
    <h2><a class="pft" href="https://profootballtalk.nbcsports.com/">Pro Football Talk</a> Top Story Scraper</h2>
</div>
<div class="card">
    <table class="table table-striped">
    <thead class="thead-dark">
        <tr>
            <th>Title</th>
            <th>Link</th>
        </tr>
    </thead>
    <tbody class="table-striped">
        {{#each result}}
        <tr>
            <td>{{title}}</td>
            <td><a href="{{link}}">{{link}}</a></td>
        </tr>
        {{/each}}
    </tbody>
    </table>
</div>